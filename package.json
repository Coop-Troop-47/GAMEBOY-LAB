{
  "name": "gbc-lab-emulator",
  "version": "2.5.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "vite build",
    "sync:artwork": "node scripts/sync-libretro-artwork.mjs",
    "preview": "vite preview",
    "test": "npm run build && node --test tests/*.test.mjs",
    "test:core": "node --test tests/gameboy-core.test.mjs",
    "benchmark:core": "node --expose-gc scripts/core-benchmark.mjs",
    "test:mealybug": "node scripts/mealybug-conformance.mjs",
    "lint": "eslint ."
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "6.0.4",
    "eslint": "10.8.0",
    "vite": "8.1.5",
    "vite-plugin-singlefile": "2.3.3"
  }
}
