{
  "name": "gbc-lab-emulator",
  "version": "1.2.5",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "vite build",
    "sync:artwork": "node scripts/sync-libretro-artwork.mjs",
    "preview": "vite preview",
    "test": "npm run build && node --test tests/*.test.mjs",
    "test:core": "node --test tests/gameboy-core.test.mjs",
    "lint": "eslint ."
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "6.0.4",
    "eslint": "10.8.0",
    "vite": "8.1.5",
    "vite-plugin-singlefile": "2.3.3"
  }
}
